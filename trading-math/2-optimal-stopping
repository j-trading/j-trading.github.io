## 37% 法则 (Optimal Stopping) —— 卖出时机的数学底线

交易中最折磨人的时刻，不是买入，而是卖出。

买错了一笔，你还可以安慰自己"逢低加仓"。但卖早了，眼睁睁看着行情再涨 30%；卖晚了，利润回吐成亏损——这种"本可以"的痛苦，才是交易员真正的心理劫难。大部分人最终的错误，不是"没看见机会"，而是在贪婪与恐惧之间反复横跳，最终在一个随机的情绪点上投降离场。

今天，我们用数学给"什么时候卖"一个硬性基准——**最优停止理论**。

---

### 1. 秘书问题：为什么"再看看"是最昂贵的话？

最优停止理论最经典的案例是**秘书问题**：

> 你要面试 100 位候选人，每次面试一人，面试后必须立刻决定"录用"或"放弃"。一旦放弃，不能回头。你该怎么选？

直觉会告诉你："先面几个看看水平，再挑最好的。"但"几个"到底是多少？

**数学的答案：37%。**

具体策略是：
1. **观察期：** 先面试前 37 位，**只看不选**，记住其中最优秀的那个作为基准。
2. **行动期：** 从第 38 位开始，只要遇到比基准更优秀的候选人，**立刻录用**。

这个策略能让你选中全局最优候选人的概率约为 **37%**——在所有可能策略中，这是数学上的最高胜率。

> **逻辑：** 37% 是"信息收集"与"果断行动"之间的最优平衡点。看得太少，你没有基准；看得太多，最优者已经从你手中溜走。

---

### 2. 公式拆解：$1/e$ 的数学之美

37% 并非经验值，它来自自然对数的底 $e$：

$$f^* = \frac{1}{e} \approx 0.368$$

其中 $e \approx 2.71828$ 是自然对数的底数。

**推导直觉：**
- 设总候选数为 $N$，观察前 $k$ 人，行动后选中全局最优的概率为 $P(k)$。
- 当 $N$ 足够大时，令 $x = k/N$，对 $P(x)$ 求极值，得到 $x^* = 1/e$。
- 此时的最大成功概率也恰好为 $1/e \approx 36.8\%$。

这个结果的优雅之处在于：**最优观察比例和最大成功率是同一个数。** 大自然似乎在告诉我们——完美选择与放下执念，本就是一回事。

---

### 3. 交易落点：从 37% 法则到卖出规则

在交易中，"面试候选人"变成了"观察价格波动"，"录用"变成了"卖出"。核心问题是：你持有一个盈利仓位，价格还在涨——**什么时候止盈最合理？**

#### 方法一：时间窗口法

假设你计划持有一笔交易最多 $T$ 天：

1. **观察期（前 37% 的时间）：** 只记录最高价，不卖出。
2. **行动期（后 63% 的时间）：** 价格再次触及或超过观察期最高价时，卖出。

```pinescript
// 37%法则 - 时间窗口止盈逻辑
holdPeriod = 30  // 最大持仓天数(可配置)
observeRatio = 1 / math.e  // ≈ 0.368

observeDays = math.floor(holdPeriod * observeRatio)  // 观察期天数 ≈ 11
barsSinceEntry = bar_index - strategy.opentrades.entry_bar_index(strategy.opentrades - 1)

// 观察期：记录最高价作为基准
if barsSinceEntry <= observeDays
    benchmarkHigh := math.max(benchmarkHigh, high)

// 行动期：触及基准即卖出
if barsSinceEntry > observeDays and high >= benchmarkHigh
    strategy.close("37% Exit", comment="Optimal Stop Hit")
```

#### 方法二：波动幅度法

如果你不想用固定时间窗口，可以基于价格波动幅度来定义"观察区间"：

1. 开仓后，等待价格走出一个完整的"观察波段"（例如从入场价上涨 $A\%$）。
2. 记录该波段中的最高价。
3. 之后只要价格从最高点回落超过 $B\%$（例如 $B = A \times (1 - 1/e) \approx 0.632A$），即卖出。

```pinescript
// 37%法则 - 波动幅度止盈逻辑
observeThreshold = input.float(5.0, "观察涨幅阈值(%)")  // A%
exitRatio = 1 - 1 / math.e  // ≈ 0.632，即回撤超过观察涨幅的63.2%时卖出

entryPrice = strategy.opentrades.entry_price(strategy.opentrades - 1)
currentGain = (high - entryPrice) / entryPrice * 100

// 观察期：涨幅未达阈值
if currentGain < observeThreshold
    peakPrice := math.max(peakPrice, high)

// 行动期：涨幅已达标，监测回撤
if currentGain >= observeThreshold
    peakPrice := math.max(peakPrice, high)
    drawdownFromPeak = (peakPrice - close) / peakPrice * 100
    exitTrigger = observeThreshold * exitRatio
    if drawdownFromPeak >= exitTrigger
        strategy.close("37% Vol Exit", comment="Volatility Stop Hit")
```

**应用技巧：**
1. **区分趋势与震荡：** 在趋势行情中，37% 法则可能卖得太早（趋势持续的时间远超预期）。建议结合赫斯特指数（Hurst Exponent）判断市场状态：趋势行情中延长观察期，震荡行情中使用标准 37%。
2. **多级止盈：** 不必全仓在同一时点卖出。可以用 37% 法则卖出 50% 仓位，剩余仓位用移动止损跟踪，兼顾"确定性"与"想象力"。

---

### 4. 算法局限：37% 法则的两个陷阱

作为算法工程师，你需要警惕：

*   **有限 vs 无限期假设：** 经典 37% 法则基于"有限候选人"的假设。但区块链市场是持续运行的，没有"最后一个候选人"。如果直接套用时间窗口法，你需要自己定义合理的持仓上限 $T$，而 $T$ 的选择本身就是一门艺术。
*   **非独立同分布：** 秘书问题的隐含假设是候选人质量独立同分布。但市场价格具有自相关性（趋势延续）和异方差性（波动聚集），历史最高价对未来并不独立。在极端行情中，"触及前高"可能是突破而非见顶，37% 法则会让你过早下车。

---

### 结语

**"完美是优秀的敌人。"** 37% 法则的核心智慧不是让你抓住最高点，而是承认一个数学事实：试图抓住最高点的代价，远大于"满意即止"的遗憾。它用 $1/e$ 告诉你——先收集足够的信息，然后在"足够好"的时刻果断出手，这不是妥协，而是最优解。

下一篇，我们将从卖出决策回到市场结构本身，探讨如何判断你眼前的行情到底是趋势还是噪音——**《赫斯特指数：你的标的是在走趋势，还是在瞎晃荡？》**。
